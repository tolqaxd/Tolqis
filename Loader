local StarterGui = game:GetService("StarterGui")
local success, executor = pcall(identifyexecutor)
executor = tostring(executor):lower()
if success and (executor:find("xeno") or executor:find("solara")) then
    StarterGui:SetCore("SendNotification", {
    Title = "Unsupported Executor",
    Text = "You are using xeno which is unsupported.",
    Duration = 10
})
end

StarterGui:SetCore("SendNotification", {
    Title = "Important!",
    Text = "Best Brainrot ESP and Auto Interaction is not working right now.",
    Duration = 10
})

local p = game.PlaceId
local s = {
    {n = "etfb", pid = 131623223084840, sc = "https://raw.githubusercontent.com/tolqaxd/Tolqis/refs/heads/main/ETFB.lua"},
 }

local function f(id)
    for _, g in ipairs(s) do
        if id == g.pid then return g end
    end
end

local m = f(p)

if not m then
    pcall(function()
        local pg = game:GetService("AssetService"):GetGamePlacesAsync()
        while not m do
            for _, pl in pairs(pg:GetCurrentPage()) do
                m = f(pl.PlaceId)
                if m then break end
            end
            if pg.IsFinished then break end
            pg:AdvanceToNextPageAsync()
        end
    end)
end

if m then loadstring(game:HttpGet(m.sc))() else warn("tolqis hub is not supported on this game.") end
